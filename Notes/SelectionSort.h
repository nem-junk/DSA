3 local variables are needed for selection sort [all int] -> J,I,Min

for loop [where first index is 0 last -> last index of array]
Loop Body [on completed return node]
        Set J to index from loop body
        Set Min to J
flow goes to another for loop [on completed -> Swap J with Min]
        first index is j
        last index is last of the array
Set int I to the index
        Loop Body -> check for index I in Array is less than index Min in array
        if true Set Min.to index I
